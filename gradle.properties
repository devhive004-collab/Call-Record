# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
org.gradle.parallel=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
org.gradle.caching=true
# Kept off: configuration-cache breaks secrets + google-services plugins on AGP 9.
# Re-enable after verifying `./gradlew assembleDebug --configuration-cache`.
org.gradle.configuration-cache=false
# Set the maximum number of workers to 4 to avoid overloading the machine.
org.gradle.workers.max=4
ksp.useKSP2=true
# AGP 9 + KSP: KSP registers its generated sources via kotlin.sourceSets,
# which AGP 9 built-in Kotlin rejects at configuration time. This flag
# restores the previous behavior (AGP-suggested workaround).
android.disallowKotlinSourceSets=false
# Set the Kotlin compiler execution strategy to in-process to avoid "Could not
# connect to Kotlin compile daemon" error.
# kotlin.compiler.execution.strategy=in-process
# WARN lets local debug builds run without google-services.json (Firebase is
# unused after Phase 6 dep cleanup). CI release must provide the file or set
# this to false to fail fast — see app/build.gradle.kts googleServices block.
googleServices.missing.passthrough=true

