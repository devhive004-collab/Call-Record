<?xml version="1.0" encoding="utf-8"?><!--
    API 31+ cloud-backup / device-transfer rules.
    Same contract as backup_rules.xml: DB may restore without its audio files;
    RecordingRepository.cleanupMissingFiles() sweeps phantom rows on startup.
    Audio is intentionally not <include>d (cloud quota + size).
-->
<data-extraction-rules>
    <cloud-backup>
        <!-- TODO: Use <include> and <exclude> to control what is backed up.
        <include .../>
        <exclude .../>
        -->
    </cloud-backup>
    <!--
    <device-transfer>
        <include .../>
        <exclude .../>
    </device-transfer>
    -->
</data-extraction-rules>
